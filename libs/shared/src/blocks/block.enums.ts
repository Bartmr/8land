export enum DynamicBlockType {
  Door = 'door',
  App = 'app',
  TrainPlatform = 'train-platform',
  // NO-OP type
  Other = 'other',
}

export enum StaticBlockType {
  Collides = 'collides',
  Text = 'text',
}
